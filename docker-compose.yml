version: "2.1"
services:

  vllm-env:
    build:
      context: ./image
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_TOKEN=${TOKEN}
    volumes:
      - ./cache:/root/.cache:rw
    command: tail -f /dev/null
    ports:
      - "8000:8000"
